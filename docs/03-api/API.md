# 接口规范

> 状态：生效（通用规范 + 接口清单）
> 最后更新：2026-09-15
> 关联文档：[`../02-architecture/ARCHITECTURE.md`](../02-architecture/ARCHITECTURE.md) · [`../07-specs/`](../07-specs/README.md)（功能规格）

接口风格为 **REST + JSON**。本文件是**对外契约的单一事实来源**：接口先在此登记，再实现于 `internal/handler/`。

---

## 1. 通用规范

### 1.1 版本管理

- 版本在**路径**中体现：`/api/v1/...`。
- **不兼容变更**必须升版本（`/api/v2/`），旧版本保留至少 1 个发布周期。
- 兼容性变更（新增可选字段）不需要升版本。

### 1.2 命名

- 路径使用**小写 + 连字符**，资源名用**复数**：`/api/v1/vms`。
- 路径表示资源层级，动词用 HTTP 方法表达，**不在路径中出现动词**。
- 查询参数与请求/响应字段统一使用**下划线**：`page_size`、`created_at`。

### 1.3 HTTP 方法语义

| 方法 | 语义 | 幂等 |
|---|---|---|
| GET | 查询，不产生副作用 | 是 |
| POST | 创建或触发动作 | 否 |
| PUT | 全量替换 | 是 |
| PATCH | 部分更新 | 否 |
| DELETE | 删除 | 是 |

### 1.4 状态码

| 状态码 | 使用场景 |
|---|---|
| 200 | 查询/更新成功 |
| 201 | 创建成功 |
| 204 | 删除成功，无返回体 |
| 400 | 参数校验失败 |
| 401 | 未认证 |
| 403 | 已认证但无权限 |
| 404 | 资源不存在 |
| 409 | 状态冲突（如重复创建） |
| 422 | 语义校验失败 |
| 428 | 高风险操作需先完成二次验证（见 [`f-10-01`](../07-specs/f-10-01-high-risk-verification.md)） |
| 429 | 触发限流 |
| 500 | 服务端异常 |
| 503 | 服务不可用 |

---

## 2. 请求约定

- 请求体统一 `Content-Type: application/json`（文件上传除外）。
- 公共请求头：

| 请求头 | 必填 | 说明 |
|---|---|---|
| Authorization | 是 | 认证凭据 |
| X-Request-Id | 否 | 链路追踪，未传则服务端生成 |
| Accept-Language | 否 | 多语言 |

- 分页参数（**统一命名，全项目一致**）：

| 参数 | 类型 | 默认 | 说明 |
|---|---|---|---|
| page | int | 1 | 页码，从 1 开始 |
| page_size | int | 20 | 每页条数，最大 100 |

- 所有**外部输入必须校验**：类型、长度、范围、枚举、格式。

---

## 3. 响应约定

### 3.1 成功响应

```json
{
  "data": {},
  "request_id": "xxx"
}
```

分页响应：

```json
{
  "data": [],
  "pagination": {
    "page": 1,
    "page_size": 20,
    "total": 100,
    "total_pages": 5
  },
  "request_id": "xxx"
}
```

### 3.2 错误响应

```json
{
  "error": {
    "code": "INVALID_PARAMETER",
    "message": "参数校验失败",
    "details": [
      { "field": "email", "reason": "格式不正确" }
    ]
  },
  "request_id": "xxx"
}
```

**规则**：
- `code` 为**稳定的机器可读枚举**，前端据此分支；`message` 可读，可随文案调整。
- 错误信息**不得泄漏**堆栈、SQL、内部路径、服务名版本等敏感信息。
- 所有响应都带 `request_id`，便于排查。

### 3.3 错误码登记

| code | HTTP | 含义 | 处理建议 |
|---|---|---|---|
| INVALID_PARAMETER | 400 | 参数不合法 | 修正参数 |
| UNAUTHENTICATED | 401 | 未认证 | 重新登录 |
| PERMISSION_DENIED | 403 | 无权限 | 联系管理员 |
| RESOURCE_NOT_FOUND | 404 | 资源不存在 | 检查 ID |
| RESOURCE_CONFLICT | 409 | 资源冲突 | 刷新后重试 |
| VALIDATION_FAILED | 422 | 语义校验失败（格式合法但业务不可接受） | 按提示修正 |
| RISK_VERIFICATION_REQUIRED | 428 | 高风险操作需先完成二次验证 | 完成验证后重放原请求 |
| RATE_LIMITED | 429 | 请求过于频繁 | 退避重试 |
| INTERNAL_ERROR | 500 | 服务内部错误 | 携带 request_id 反馈 |
| SERVICE_UNAVAILABLE | 503 | 依赖不可用（如节点离线） | 稍后重试 |

### 3.4 高风险操作的 428 响应结构

被保护的操作在缺少有效许可时返回 428，且**响应体额外携带 `data`**——
前端要靠它才能发起验证（渲染哪种输入方式、用哪个 challenge）：

```json
{
  "error": { "code": "RISK_VERIFICATION_REQUIRED", "message": "该操作需要完成二次验证" },
  "data": {
    "action": "vm.delete",
    "challenge_id": "eyJ1Ijo3...",
    "methods": [{ "method": "totp", "label": "验证器动态码" }],
    "expires_at": "2026-09-15T12:05:00Z"
  },
  "request_id": "..."
}
```

`methods` 为**空数组**（而非 `null`）表示该账号尚未绑定任何验证方式：前端据此
提示「需先绑定」，而不是渲染一个没有输入框的弹框。

验证通过后，前端在**重放原请求**时通过请求头 `X-Risk-Grant` 携带许可。许可
**一次性、2 分钟有效、绑定会话，消费即失效**——重放一次即作废，因此「验证
一次、连续删多次」不成立；而批量操作本就是一次请求，不会带来重复验证。

> **API Key 的差异**（[`f-10-01`](../07-specs/f-10-01-high-risk-verification.md) R-012）：
> 通过 API Key 调用的请求不触发交互式验证——API 无交互能力，且 Key 本身即长期
> 凭据。这是**显式取舍**，不是遗漏。当前系统仅支持会话认证，尚无 API Key 能力，
> 该规则待其交付时落实并在本节更新。

> 新增错误码必须先在此登记，禁止临时编造。

---

## 4. 安全要求

- 所有接口默认**需要认证**，公开接口必须显式标记并说明理由。
- 每个数据访问接口都要做**归属校验**（防止越权访问他人数据）。
- 敏感字段（口令、令牌、证件号）在响应中**一律脱敏或不下发**。
- 接口必须防重放（幂等键或时间戳 + 签名，按场景）。
- 涉及写操作的接口应校验幂等性，避免重复提交产生重复数据。

---

## 5. 接口清单

| 编号 | 方法 | 路径 | 说明 | 认证 | 状态 | 关联功能 |
|---|---|---|---|---|---|---|
| API-001 | GET | `/health` | 健康检查，含数据库连通性探测 | 否 | 已实现 | 探活 / 就绪检查 |
| API-002 | POST | `/api/v1/nodes/registration-tokens` | 生成一次性注册令牌并创建待接入节点；**明文令牌只返回一次** | 是（管理员） | 已实现 | F-6-01 |
| API-003 | GET | `/api/v1/nodes` | 节点列表（元数据 + 运行态；状态由心跳推导） | 是（管理员） | 已实现 | F-6-02 |
| API-004 | GET | `/api/v1/nodes/:id` | 节点详情（含能力清单） | 是（管理员） | 已实现 | F-6-02 |
| API-005 | DELETE | `/api/v1/nodes/:id` | 移除节点（软删除，保留审计与历史引用）；**高风险操作，需二次验证** | 是（管理员） | 已实现 | F-6-01 |
| API-070 | PATCH | `/api/v1/nodes/:id/maintenance` | 进入 / 退出维护模式。**同步生效，不进任务队列**——维护模式纯粹是控制面的标志（f-2-01 Q-009：意义是「不再引入变更」，而非「停止业务」），做成任务会制造一个「界面说维护中、操作仍被受理」的危险窗口。不需要二次验证：进入是收紧、退出是恢复，两者都可逆 | 是（管理员） | 已实现 | F-6-05 |
| API-071 | POST | `/api/v1/nodes/:id/vpc-switches` | 新建虚拟交换机。**走任务队列**——建网桥是宿主机上的实际操作，接口不同步等待；记录由执行器在节点成功后写入。网桥名由控制面生成（Linux 接口名上限 15 字符，用名称直接做网桥名会被内核**静默截断**） | 是（管理员） | 已实现 | F-4-02 |
| API-072 | PATCH | `/api/v1/vpc-switches/:id` | 修改交换机。`name` / `mode` / `vlan_id` / `cidr` / `gateway_ip` / `dhcp_start` / `dhcp_end` / `uplink_if`。网桥名**不可变**——改它会让控制面指向一个宿主机上不存在的网桥，原网桥成为孤儿。系统基础网络拒绝修改 | 是（管理员） | 已实现 | F-4-02 |
| API-073 | DELETE | `/api/v1/vpc-switches/:id` | 删除交换机。**占用检查同步完成**：还有网卡接在上面时立即返回 409 并给出数量，而不是排进队列等几分钟后再失败——那时用户多半已经在做别的事了 | 是（管理员） | 已实现 | F-4-02 |
| API-074 | POST | `/api/v1/vms/:id/rescue` | 进入救援模式：调整引导顺序、盘型与网卡后从救援镜像启动。**必须已关机**——救援改的是硬件配置，热改会让控制面记录与虚拟化层实际分叉。进入前保存配置快照供退出还原。不需要二次验证：救援不破坏数据，且它正是故障时的出路 | 是 | 已实现 | F-2-12 |
| API-075 | DELETE | `/api/v1/vms/:id/rescue` | 退出救援并按快照还原。同样需先关机。快照缺失时**拒绝退出**而不是用默认值「还原」——后者会静默改掉用户的引导顺序，而他无从判断那是不是自己原来的配置 | 是 | 已实现 | F-2-12 |
| API-076 | GET | `/api/v1/templates` | 模板列表。可见性由服务层过滤：管理员看全部，普通用户看**已发布**的加**自己创建的**。`only_ready=true` 只返回可用于创建的 | 是 | 已实现 | F-3-01 |
| API-077 | GET | `/api/v1/templates/:id` | 模板详情。私有模板对非所有者返回 **404 而非 403**——403 会确认「这个 ID 存在」，让 tenant 能通过枚举推断出别人有多少模板 | 是 | 已实现 | F-3-01 |
| API-078 | POST | `/api/v1/templates` | 从一台虚拟机的系统盘制备模板。**要求源虚拟机关机**（用实时探测判定，不用投影）：运行中的系统盘在被复制的同时还在被写入，做出来的模板是崩溃一致性的——而它会被**反复克隆**，于是每一台克隆机开机都要做 fsck。走任务队列 | 是 | 已实现 | F-3-01 |
| API-079 | PATCH | `/api/v1/templates/:id` | 修改模板（发布 / 停止提供克隆 / 备注）。**同步生效**——都是纯控制面元数据，做成任务会制造「界面说已发布、实际还没发布」的窗口，而发布与否决定了别人能不能用 | 是 | 已实现 | F-3-01 |
| API-080 | DELETE | `/api/v1/templates/:id` | 删除模板。**仍有链式克隆依赖时同步拒绝**并给出数量——父盘一删，那些虚拟机的数据就不可用了，而且不会立刻报错，要等到下次开机或读到未缓存的数据块。派生模板同样阻止删除 | 是 | 已实现 | F-3-02 |
| API-081 | POST | `/api/v1/vms/:id/reinstall` | 重装系统：用模板重建系统盘，**保留硬件配置、数据盘与主网口绑定**。要求已关机、无在途任务、**无未清理的备份**（第二次重装会覆盖上一次的备份，而那是用户唯一的退路）。原系统盘先备份再重建——顺序反了会让失败时的还原变得不可能 | 是（高风险） | 已实现 | F-2-11 |
| API-082 | DELETE | `/api/v1/vms/:id/reinstall/backup` | 清理重装留下的备份盘。要求已关机（备份可能是系统盘的底层链），**不需要二次验证**——删的是已不再被使用的备份，当前运行不受影响 | 是 | 已实现 | F-2-11 |
| API-098 | POST | `/api/v1/vms/:id/migrate` | 跨节点迁移。**要求关机**——运行中迁移会让磁盘在被写入的同时被复制，两侧都不可用。受理时同步检查目标节点是否维护中、**静态地址与端口转发在目标节点上是否冲突**（这两类资源在节点内唯一，换一台宿主机就可能撞上别人），有冲突时**列出具体是哪一项**。不需要二次验证：失败时源侧保留，虚拟机在原处仍可用 | 是 | 已实现 | F-2-09 |
| API-099 | GET | `/api/v1/vms/:id/migrations` | 迁移记录。回答「这台机器原来在哪台宿主机上」——排查存储、网络、性能问题时第一条要看的东西，而 `node_id` 只记录「现在在哪」。`result` 说明跟着搬了些什么 | 是 | 已实现 | F-2-09 |
| API-093 | GET | `/api/v1/imports/format` | 按文件名推断源格式。**由后端推断而不是信前端传的值**：扩展名是用户唯一会认真看的东西 | 是 | 已实现 | F-2-13 |
| API-094 | POST | `/api/v1/imports/parse` | 解析待导入文件，返回配置预览。**发生在受理之前**——f-2-13 要求「先解析预览再创建」，而预览的意义就是让用户在确认前看到将要创建的是什么。OVA 的值来自包内 OVF，裸镜像的值来自用户；预览里标出**每一项的来源** | 是 | 已实现 | F-2-13 |
| API-095 | GET | `/api/v1/imports` `/api/v1/imports/:id` | 导入记录。普通用户只看自己的，管理员看全部；非所有者返回 **404 而非 403**（403 会确认 ID 存在）。记录在**受理时**即创建——格式转换可能处理几十 GB | 是 | 已实现 | F-2-13 |
| API-097 | DELETE | `/api/v1/imports/:id` | 删除导入记录。进行中时拒绝。**只删记录、不删产出的模板**——模板可能已被克隆成多台虚拟机，而那些虚拟机在磁盘上依赖的可能正是它 | 是 | 已实现 | F-2-13 |
| API-096 | POST | `/api/v1/imports` | 受理导入。**本面板不接收真实文件内容**：只提交文件名、大小与格式（浏览器可直接读到的元数据）。整条链路可验证，唯独「字节怎么从浏览器到宿主机」留空——那一段必须真实实现。产物是**模板**而非虚拟机：用户接下来多半是「用它开几台机」 | 是 | 已实现 | F-2-13 |
| API-320 | GET | `/api/v1/host/passthrough` | PCIe 设备清单与 IOMMU 状态。**同组成员必须一起返回**：IOMMU 分组里的设备只能**一起**直通，而分组关系取决于主板拓扑与 BIOS 设置——**只有节点探测得到**。用户选了显卡之后发现鼠标键盘也一起被拿走了，正是「同组」的表现，而界面上必须在选择的那一刻就说出来。不可直通的设备给出原因（如「与宿主机管理网络共用，直通会让面板失去连接」） | 是（管理员） | 已实现 | — |
| API-321/322 | POST | `/api/v1/host/passthrough/bind`、`/unbind` | 绑定设备到 vfio-pci / 解绑回宿主驱动。它会**改变宿主机上设备的归属**——把一块卡从宿主驱动抢过来，宿主就看不到它了；若那块卡正被宿主使用（如唯一的网卡），绑定会让宿主机**当场失去网络**。**解绑前必须确认没有虚拟机在用**：解绑一块正被使用的卡等于在那台机器运行中把它的「显卡」拔掉，而用户点这个按钮时想的多半是「我不需要了」 | 是（管理员） | 已实现 | — |
| API-323 | GET | `/api/v1/vms/:id/passthrough` | 虚拟机挂载的直通设备。`device_desc` 与 `iommu_group` 是**快照**：设备被拔掉或换槽位之后探测结果里就没有它了，而用户需要知道「这台机器当时挂的是什么」 | 是（管理员） | 已实现 | — |
| API-324 | POST | `/api/v1/vms/:id/passthrough` | 挂载直通设备。**要求虚拟机处于关机状态**——直通设备不支持热插拔，失败方式是一台机器卡在半启动状态。**同组冲突检查**：一个 IOMMU 分组只能属于一台虚拟机，不检查的话用户把同组两块卡挂给两台机器，而 libvirt 只会在第二台启动时失败，那时第一台已经跑起来了。审计里记 `iommu_group`：事后排查「为什么这台机器起不来」时它是唯一能解释的东西 | 是（管理员） | 已实现 | — |
| API-325 | DELETE | `/api/v1/vms/:id/passthrough` | 卸载直通设备。同样要求关机 | 是（管理员） | 已实现 | — |
| API-310 | GET | `/api/v1/host-firewall` | 宿主机防火墙的策略与规则（F-4-11 第一层）。**与 `/firewall/*` 是两套**：这一套保护宿主机自己与面板（SSH、面板端口），那一套管虚拟机的入站流量。返回里含两条 `fixed: true` 的**合成保护规则**（面板端口、SSH）——它们不在表里，由面板按当前配置算出来：端口存进表之后改配置那条就过期了，而一条过期的「保护规则」比没有更糟，它保护着一个不再监听的端口 | 是（管理员） | 已实现 | F-4-11 |
| API-311 | PATCH | `/api/v1/host-firewall/policy` | 更新策略（启用、默认处置、管理白名单）。**白名单入审计**：它是唯一一条能从错误规则里把人救回来的路。默认**不启用**——默认拒绝是「启用之后」的行为，一上来就启用会让所有现网节点在下一轮收紧，而那可能切断正在使用的连接 | 是（管理员） | 已实现 | F-4-11 |
| API-312 | POST | `/api/v1/host-firewall/rules` | 新建规则。新建的规则**永远不是保护的**——保护标记只能由服务端按配置合成。接受客户端传入的话，用户可以把一条保护规则改成不保护、删掉它，然后把自己锁在门外，而整个过程看起来完全合法。ICMP **拒绝填端口**：填了会得到一条看起来有限制、实际没有的规则 | 是（管理员） | 已实现 | F-4-11 |
| API-313 | DELETE | `/api/v1/host-firewall/rules/:id` | 删除规则。保护规则**服务端强制拒绝**（409），不看界面传了什么——让界面决定能不能删，等于把一个**不可恢复**的操作交给一次点击：删掉之后管理员可能立刻连不上面板，而那时他已经没有界面可以把它加回来 | 是（管理员） | 已实现 | F-4-11 |
| API-314 | GET | `/api/v1/host-firewall/precheck` | 预览将要下发的规则。**只读**。它在**白名单为空且默认拒绝**时给出显式警告——那是「任何来源都连不上这台机器」的组合，包括 SSH 与面板，而应用之后用户已经进不去界面把它改回来 | 是（管理员） | 已实现 | F-4-11 |
| API-315 | POST | `/api/v1/host-firewall/apply` | 应用。**必须带回预览时的 version**——没有「不传版本就跳过校验」这一说，那是个绕过：调用方只要省略 version 就完全得不到保护，而接口看上去一切正常 | 是（管理员） | 已实现 | F-4-11 |
| API-316 | POST | `/api/v1/host-firewall/rollback` | **紧急回滚**：撤销本系统写入的全部宿主机规则。**不校验版本、不要求二次验证**——它要能在「已经出事了」的那一刻还能用：应用之后连不上面板或 SSH 断了、而用户又进不去宿主机时，这是唯一的自救入口。回滚时刻单独记一列，因为「这条策略最近被回滚过」是排查「为什么规则和我配的不一样」时第一个要看的东西 | 是（管理员） | 已实现 | F-4-11 |
| API-317 | GET | `/api/v1/host-firewall/connections` | 当前的入站连接。`own` 标记「这条是你自己那条」——由**控制面**填而不是节点：节点无从知道请求方是谁，而关掉自己那条连接会让人以为面板挂了 | 是（管理员） | 已实现 | F-4-11 |
| API-318 | POST | `/api/v1/host-firewall/connections/close` | 关闭一条连接。它会**切断正在进行的会话**——包括管理员自己那条，因此界面上必须把「这条属于谁」写清楚 | 是（管理员） | 已实现 | F-4-11 |
| API-300 | GET | `/api/v1/resource-quotas` | 资源配额与**当前周期实际用量**（F-4-10）。用量不在这张表里——它来自按天累计的统计表现算，因此用户看到的数字与「为什么被限速」是同一个东西。`limit_value` 为 0 表示不限（默认）。`status` 有三档：ok / warned（达到 80% 预警线）/ limited（**已处置**）——warned 与 limited 必须分开，否则用户在网络变慢时无法判断是自己用超了还是会话出了问题 | 是（管理员） | 已实现 | F-4-10 |
| API-301 | PUT | `/api/v1/resource-quotas` | 设置配额。默认处置是**限速而非断网**——断网会让业务直接中断，选它的人应当是有意为之。**提高上限会清除该维度的处置状态**：不清的话，用户明明已经合规，网络却还是慢的，而界面上显示「已超限」。周期按 **UTC** 的自然月分桶（与运行时长同一口径），跨月自动重置——不重置的话，上个月被限速的用户在新的一月里仍然限着，而那时他的用量是 0 | 是（管理员） | 已实现 | F-4-10 |
| API-302 | DELETE | `/api/v1/resource-quotas/:id` | 删除配额。删除意味着**回到不限**，因此必须撤掉节点上已经生效的处置——否则用户在界面上看到「没有配额」，而那条限速规则还挂着 | 是（管理员） | 已实现 | F-4-10 |
| API-290 | GET | `/api/v1/settings/log/status` | 日志运行状态（F-9-02）。含**各级别条数统计**——它直接回答打开日志页时最想问的第一个问题「最近有没有错误」，而不必先翻一屏。`file_path` 为空表示未落盘（开发环境是合法配置，不是故障） | 是（管理员） | 已实现 | F-9-02 |
| API-291 | GET | `/api/v1/settings/log/read` | 在线查看最近的日志。**只读内存环形缓冲，不扫磁盘**：在线查看要即时响应，而扫一遍几百 MB 的日志文件会让页面卡上几秒。更早的内容用导出。可按级别与关键字过滤 | 是（管理员） | 已实现 | F-9-02 |
| API-292 | PUT | `/api/v1/settings/log/level` | 调整日志级别。**记审计**：调到 DEBUG 会让日志量显著上升（磁盘消耗同步加快，暴露面也更大），事后要能回答「是谁在什么时候把它打开的」。不要求重启——它恰恰是最需要临时调高的一项（「把 debug 打开看看」），要求重启会让它形同虚设 | 是（管理员） | 已实现 | F-9-02 |
| API-293 | GET | `/api/v1/settings/log/export` | 导出日志（含轮转文件，**从旧到新**拼接）。导出的内容是**脱敏后**的：脱敏发生在写入时，因此磁盘上那份本身就不含明文，这个接口不需要再做一遍处理，也**不可能**因为「忘了处理」而泄漏 | 是（管理员） | 已实现 | F-9-02 |
| API-294 | POST | `/api/v1/settings/log/delete` | 清理日志。**不删除当前文件本身，只截断**：删掉它会让正在运行的服务失去写入位置，表现为「清理之后日志再也不出现了」——而那时恰恰最需要日志。清理不可逆，因此记审计时把「清掉了多少字节」一并记下 | 是（管理员） | 已实现 | F-9-02 |
| API-280 | GET | `/api/v1/version` | 面板版本、构建信息与依赖清单（F-9-05）。**全部字段来自二进制的构建信息，一个都没有手写**——手写的清单在第一次 go mod tidy 之后就漂移了，而它恰恰是排查「这个版本用的是哪个库」时要看的东西；一份漂移的清单比没有更糟，它给出一个看起来很具体的错误答案。依赖清单反映的是**这个二进制里实际链了什么**，而不是 go.mod 里写了什么（后者含大量不会被链接的间接依赖）。被 replace 的模块单独列出：一个 replace 意味着实际跑的不是官方版本，这正是「本地能跑、线上出问题」最常见的原因之一。没有构建信息时**如实留空**而不是编一个版本号 | 是 | 已实现 | F-9-05 |
| API-270 | GET | `/api/v1/diagnostics/categories` | 可导出的诊断分类（F-9-03）。每类带 `sensitive` 标记与说明——用户有权在导出前知道日志里含操作者与来源 IP | 是（管理员） | 已实现 | F-9-03 |
| API-271 | GET | `/api/v1/diagnostics/export` | 按分类生成并下载排障包（zip）。**脱敏发生在生成时**，而不是提醒用户自己检查：排障包的用途就是发给别人，任何依赖用户"记得先删密码"的安排都会失败。脱敏清单**复用 settings.Spec.Secret 且只有这一份**——自己再维护一份"哪些键敏感"迟早会漏掉新增项，而漏掉的表现是某个密钥被原样导出且没有任何地方报错。**同步生成、不落盘**：诊断包是瞬时快照，不落盘就不存在"忘记删"（与抓包不同，抓包必须落盘因为它要跑一段时间）。有硬上限且**截断写进 MANIFEST**：不说的话离线分析的人会把"只看到最后 2000 条日志"当成"这段时间只有这些" | 是（管理员） | 已实现 | F-9-03 |
| API-260 | GET | `/api/v1/captures` | 节点上的抓包记录（F-4-12）。`status` 是三态：进行中（带服务端算好的**剩余秒数**）与可下载——抓包是异步的，下发之后要等 duration 秒才有文件，这期间显示"文件不存在"会让用户以为失败了。租户只看自己发起的：抓包内容含明文流量，而一个网口上可能同时有多个租户的机器 | 是 | 已实现 | F-4-12 |
| API-261 | POST | `/api/v1/captures` | 发起抓包。**必须有时长**（5~300 秒）：不限时的抓包会把宿主机磁盘写满，而且用户会忘记停——它的失败不是"没抓到"而是"把宿主机写挂了"。**过滤器是注入面**（最终会拼进 tcpdump 命令行），控制面做黑白名单双重校验——节点侧的转义是第二道防线不是第一道。同一节点同时最多 3 个：抓包占 CPU 与磁盘带宽，而它跑在生产节点上 | 是 | 已实现 | F-4-12 |
| API-262 | DELETE | `/api/v1/captures/:id` | 删除抓包。**文件与控制面记录一起删**：只删记录会让一份含明文流量（密码、会话令牌）的文件永远留在宿主机上，而界面上已经看不到它了。文件还没生成时不必下发节点 | 是 | 已实现 | F-4-12 |
| API-250 | GET | `/api/v1/port-security` | 节点上的端口安全策略（F-4-08）。三项保护性质**不同**：防伪造是安全边界且是其它隔离措施的前提（不防欺骗的话，按地址区分机器的策略都能被绕过）；隔离代价大（同网段内所有机器互不可见）；限速是资源保护而非安全措施。`applied_at` 为空表示尚未生效——与「已启用」是两回事 | 是（管理员） | 已实现 | F-4-08 |
| API-251 | POST | `/api/v1/port-security/preview` | 预检。**只读**。有两件必须在按下「启用」之前就看到的事：**能力是否具备**（依赖 Open vSwitch，节点上很可能没装，等到点下去才说"不支持"时用户已经以为配好了），以及**将要下发的流表**——必须来自节点，因为最终规则取决于该网口所属网桥、已有流表与端口号 | 是（管理员） | 已实现 | F-4-08 |
| API-252 | POST | `/api/v1/port-security` | 配置并启用。缺必需能力时**直接拒绝**，而不是收下配置标成「已启用」——不会生效的策略会让用户以为攻击面已经收住了。有警告而未确认时**不执行也不报错**，而是返回预检结果。下发的是**期望状态**而非增量指令：节点上的实际状态可能被手工改过，增量指令在那种情况下会收敛到错误结果 | 是（管理员） | 已实现 | F-4-08 |
| API-253 | DELETE | `/api/v1/port-security/:id` | 停用。**先把配置清零再下发**——下发的是期望状态而它来自这条记录，不先清零的话下发的还是原来那份，节点上一条规则都不会被撤销，而界面会显示「已停用」。不需要二次验证：关掉之后同网段立刻恢复互通 | 是（管理员） | 已实现 | F-4-08 |
| API-240 | GET | `/api/v1/schedulers` | 周期性调度器及其最近事件（F-7-04）。列表**来自注册表**（代码里登记的"有哪些东西在跑"），而不是来自事件表——事件表只在该做事的时候才有行，只靠它会让一个正常但最近无事可做的调度器凭空消失，而用户会把它读成故障。每个调度器带上**周期**：界面上没有别的字段能告诉用户"多久做一次"，而它是判断这东西是否还活着的唯一依据 | 是（管理员） | 已实现 | F-7-04 |
| API-241 | GET | `/api/v1/scheduler-events` | 调度事件流，可按调度器与结果筛选。**只记录实际发生的调度动作，不记录普通轮询**——一个每 30 秒醒一次的调度器一天会醒 2880 次，若每次都记一条"检查过，无事发生"，真正有价值的那条（"清理了 500 个过期任务"）会淹没在两千多条噪声里。事件冗余保存**当时的名字**：调度器是代码里的常量，改名后历史事件应当保留当时那个名字 | 是（管理员） | 已实现 | F-7-04 |
| API-230 | GET | `/api/v1/monitor/host` | 宿主机的指标序列（F-8-01）。数据由**独立采集器按固定间隔写入**，不是「用户看页面时顺便采一次」——后者的密度由点击行为决定，用它算「过去一周的负载」会得到与真实情况毫无关系的结果，而它看起来像一份正常的图表。**采集失败时不写记录**（写 0 会让图表显示「CPU 为 0%」，那看起来像机器很闲，而不是没采到）。超量时**按时间等距抽稀**而不是截断——截断会让图表画出一段「最近的」数据却看起来覆盖了整个查询范围 | 是（管理员） | 已实现 | F-8-01 |
| API-231 | GET | `/api/v1/vms/:id/monitor` | 虚拟机的指标序列（F-8-02）。只采**运行中**的机器：停机机器没有指标可读，给它写记录会让图表上出现一条贴着 0 的线 | 是 | 已实现 | F-8-02 |
| API-232 | GET | `/api/v1/vms/:id/runtime` | 虚拟机的运行时长（F-8-06）。数据来自采样器**按天累计**的 `vm_runtime_daily`——它是超限处置与 F-1-08 配额里「运行时长」维度的共同数据源。按天分桶而不是存一个总数：配额通常按月算，只存总数就无法回答「上个月用了多少」。日期统一按 **UTC** 切分——按本地时区切会让历史数据的分桶随时区设置而变，而配额是按月结算的 | 是 | 已实现 | F-8-06 |
| API-220 | GET | `/api/v1/vms/:id/tags` | 一台虚拟机的标签（F-2-16）。标签与分组解决的不是同一件事：分组**互斥**（一台机器只属于一个组），标签**非互斥**（可以有任意多个）——合并会立刻遇到矛盾，一台机器既是「生产」又是「数据库」而分组只能放一个 | 是 | 已实现 | F-2-16 |
| API-221 | PUT | `/api/v1/vms/:id/tags` | **整体替换**标签，而不是增量增删。界面改完直接保存，而 add/remove 逐个操作会让「加了又删、删了又加」的中间态被如实写进数据库与审计流水，让那条记录读起来很难理解。替换在事务里完成（分两步会出现「旧的已删、新的没写」的瞬间，界面上看就是标签丢了）。服务端去重——唯一索引也会拦，但那时的报错与「你写了两个一样的标签」联系不起来 | 是 | 已实现 | F-2-16 |
| API-222 | GET | `/api/v1/tags` | 该调用者**可见范围内**的全部标签及使用次数。计数让「生产 (12)」比「生产」有用——能判断这个标签是不是只有一台机器的孤儿标签。按可见范围返回，否则租户能通过它推断出别人的机器命名习惯与数量 | 是 | 已实现 | F-2-16 |
| API-223 | GET | `/api/v1/tags/vms` | 按标签查虚拟机 id。不由界面自己筛：列表是分页的，前端只拿得到当前页，而「哪些机器带这个标签」必须看全量 | 是 | 已实现 | F-2-16 |
| API-210 | GET | `/api/v1/users` | 用户列表（F-1-07）。含最后登录时间——**一次查完**，逐个查会把一次列表请求变成上百次往返。`last_login_at` 为空表示从未登录，那通常说明这个账号已经不需要了 | 是（管理员） | 已实现 | F-1-07 |
| API-211 | POST | `/api/v1/users` | 创建用户。新建账号是 **pending（待激活）** 而非直接可用——那给了一个「先建好、确认无误、再让人用」的中间状态；并**强制首次登录改密**，因为管理员给的是初始密码。**审计流水里绝不记密码**（哪怕是初始密码，它会让审计变成后门）。不允创建系统账号 | 是（管理员） | 已实现 | F-1-07 |
| API-212 | PATCH | `/api/v1/users/:id` | 编辑用户资料。**不含密码**——改密是独立且更敏感的动作（会让已登录会话全部失效），塞进「编辑资料」会让一次无心的保存造成一次全员登出。**不能改自己的角色**；降级管理员时校验「不是最后一个可用的」 | 是（管理员） | 已实现 | F-1-07 |
| API-213 | PUT | `/api/v1/users/:id/status` | 封禁 / 解封。这是本模块最核心的一处语义：封禁是**级联动作**（置状态 → 撤销会话 → 停运行中的虚拟机），且**单步失败仅告警**——封禁的实质是置状态，那一步就达成了目的；后两步是减少暴露面的加固。让加固的失败把整个操作判为失败，会让界面显示「封禁失败」，**而那人其实已经被封了**。级联失败时仍记为审计成功（同理由）。不能封禁自己；幂等 | 是（管理员） | 已实现 | F-1-07 |
| API-214 | DELETE | `/api/v1/users/:id` | 删除用户（**软删除**）。硬删除会让审计流水里的 `operator_id` 指向一个不存在的用户，那些记录就再也解释不清了。**名下有虚拟机时拒绝**并给出数量——直接删账号会让那些机器变成无人管理的资源。不能删除自己；不能删最后一个可用管理员 | 是（管理员） | 已实现 | F-1-07 |
| API-200 | GET | `/api/v1/audit` | 审计流水查询（F-1-12）。在此之前审计**只有写入没有读取**——`audit.Recorder` 一直在记，但没有任何人能查。**租户只能看自己的记录**，且看不到系统动作（`operator_id` 为空）；这条隔离在**服务层**做，之后无论谁加新入口都绕不过它。动作与资源类型支持**前缀匹配**（动作名是分层的，用户想看的是"所有快照相关的"）。LIKE 通配符被转义并带 `ESCAPE` 子句。单页上限 200，超出时明确返回 `truncated` ——普通列表少几条无所谓，**审计少几条会让「没发生过」这个结论变成错的** | 是 | 已实现 | F-1-12 |
| API-201 | GET | `/api/v1/audit/:id` | 读取一条记录。别人的记录返回 **404 而非 403**（403 会确认"这个 id 存在"，让人能通过枚举推断出系统里有多少条记录、以及它们属于谁） | 是 | 已实现 | F-1-12 |
| API-202 | GET | `/api/v1/audit/facets` | 可用筛选项（动作名、资源类型）。**按调用者的可见范围**返回——如果列表是全库的，一个租户就能通过它推断出系统里存在哪些功能被用过，包括只属于管理员的那些。由服务端给出而不是界面写死：动作会随功能增加而变化 | 是 | 已实现 | F-1-12 |
| API-190 | GET | `/api/v1/networks` | 网络底座完整状态（F-4-13）。**几乎不会失败**：探测不到节点、桥列表读不出来，都以字段形式返回（`probe_ok` / `probe_error` / `bridges_error`），整体仍是 200——这是「网络配置失败不得阻断主流程」的具体实现。用户点进这个页面本来就是为了看网络出了什么问题，给他白屏等于把唯一的诊断入口也关掉了。`degraded_reasons` 会**具体列出**哪些网络受 OVS 缺失影响，而不是笼统说一句「已降级」 | 是（管理员） | 已实现 | F-4-13 |
| API-191 | GET | `/api/v1/networks/bridges` | 只返回桥列表 | 是（管理员） | 已实现 | F-4-01 |
| API-192 | POST | `/api/v1/networks/bridges` | 新建网络。**不接物理口**——那是独立且更危险的操作，合成一步会让「先建好、确认没问题、再接物理口」这个次序无法表达。空交换机拒绝启用内置 DHCP；启用 DHCP 必须给出网段与网关（DHCP 要告诉来客网关是谁，没有它就出不了网）。**下发失败不回滚记录**，而是标成 `error` 并带上具体原因——留着用户才能看到「建了但没起来」然后去修复 | 是（管理员） | 已实现 | F-4-01 |
| API-193 | DELETE | `/api/v1/networks/bridges/:id` | 删除网络。**系统预置网络不可删**：新建虚拟机默认接的就是它，删掉等于让「新建」从可用变成不可用，而用户不会立刻把这两件事联系起来 | 是（管理员） | 已实现 | F-4-01 |
| API-194 | POST | `/api/v1/networks/bridges/:id/uplink` | **物理口入桥**——网络模块里最危险的操作：它会把物理口的 IP 配置重置，若那是管理口，操作者**当场失联**且没有任何界面路径可以改回来。两道防线：**显式确认**（防「没想清楚就点了」）与**自动回滚窗口**（防「确认之后才发现不行」）。窗口执行者在**节点侧**——控制面是通过网络下发指令的，而这个操作的结果恰恰可能是网络断掉 | 是（管理员） | 已实现 | F-4-01 |
| API-195 | POST | `/api/v1/networks/bridges/:id/uplink/confirm` | 确认保持，取消自动回滚 | 是（管理员） | 已实现 | F-4-01 |
| API-196 | DELETE | `/api/v1/networks/bridges/:id/uplink` | 摘出物理口。**不做确认、不做预检**——一个已经切断管理通道的桥要能一键摘掉 | 是（管理员） | 已实现 | F-4-01 |
| API-197 | POST | `/api/v1/networks/repair` | **修复**（F-4-13）。它是**幂等的收敛动作**，而不是「重试上一次失败的操作」：重试一件已经失败的事通常不会得到不同结果，而收敛会先看清现状再补差异。`fixed` 与 `remaining` **分开返回**——只报「修好了什么」会让用户以为没事了，只报「还有问题」又看不出这次操作做了什么。修复本身失败时**不返回错误**，而是写进 `remaining`：用户点修复多半是因为网络已经坏了，此时再收到 500 对他没有帮助 | 是（管理员） | 已实现 | F-4-13 |
| API-180 | GET | `/api/v1/port-mirrors` | 节点上的端口镜像规则（F-4-09）。`watchdog_seconds_left` 由**服务端**算好下发，而不是让界面拿时刻去减本地时间——客户端时钟不准是常态，而「还剩 3 分 12 秒」必须可信 | 是（管理员） | 已实现 | F-4-09 |
| API-181 | POST | `/api/v1/port-mirrors` | 新建镜像规则（多来源接口 → 多目标空交换机，方向可选）。默认**不启用**。创建与启用分开是刻意的：创建只写控制面，启用才会动宿主机的网络。重复来源被拒（**静默的流量放大**，在抓包里表现为「每个包都出现两次」，容易被误判成对端在重传）；同一对象不能既是来源又是目标 | 是（管理员） | 已实现 | F-4-09 |
| API-182 | PATCH | `/api/v1/port-mirrors/:id` | 修改规则。**生效中拒绝修改**——直接改等于在一次操作里同时撤掉旧的、建起新的，而新的那一段没有被看门狗保护 | 是（管理员） | 已实现 | F-4-09 |
| API-183 | DELETE | `/api/v1/port-mirrors/:id` | 删除规则。生效中拒绝 | 是（管理员） | 已实现 | F-4-09 |
| API-184 | GET | `/api/v1/port-mirrors/:id/precheck` | 启用前的静态检查：来源/目标是否与其它生效中的镜像重叠、方向是否为双向。**这只是一层静态检查**——真正的环路取决于宿主机上的实际拓扑，那只有节点知道；因此预检通过**不代表一定安全**，兜住剩下那些的是看门狗。返回里混了一句**背景说明**（预检能力的边界），它不会被当作风险拦住启用 | 是（管理员） | 已实现 | F-4-09 |
| API-185 | POST | `/api/v1/port-mirrors/:id/enable` | 启用镜像并**在同一次下发里**建立看门狗。这正是 PRD「启用前建立自动回滚看门狗」的唯一可行解释：分两次会留下一个「已生效、没兜底」的窗口，而如果网络恰好在那几秒里被镜像打垮，就再也没有东西能撤销它。**看门狗由节点计时**——控制面通过网络下发指令，而端口镜像配错恰恰会打垮网络，那时控制面自己也发不出回滚指令。窗口 60~1800 秒，**不能设为「无」** | 是（管理员） | 已实现 | F-4-09 |
| API-186 | POST | `/api/v1/port-mirrors/:id/confirm` | 确认「保持」，取消看门狗。**可选动作**：不做它镜像会被自动撤销——安全性靠的是那个默认行为，而不是用户的记性。不需要二次验证：它只是让一个已生效的东西继续生效 | 是（管理员） | 已实现 | F-4-09 |
| API-187 | POST | `/api/v1/port-mirrors/:id/disable` | 关闭镜像。**不做确认、不做预检**，且**幂等**——一个正在打垮网络的镜像，用户需要的是一键关掉，而那时他会连点好几次 | 是（管理员） | 已实现 | F-4-09 |
| API-170 | GET | `/api/v1/api-keys` | 当前用户的 API 凭证信息（前缀、来源白名单、到期、最后使用）。**不回传明文也不回传哈希**——明文只在生成时返回过一次，哈希回传只会让它出现在开发者工具与前端缓存里 | 是 | 已实现 | F-1-10 |
| API-171 | POST | `/api/v1/api-keys` | 生成或**轮换**凭证。**一人一个**（数据库唯一索引），重新生成即轮换、旧的立即失效。响应是**唯一一次**携带明文的地方——库里只存 SHA-256，服务端之后再也拿不到它。哈希用 SHA-256 而非 argon2：Key 是 256 位随机串（不存在"猜得到"），而它在**每个请求**上都要校验一次，慢哈希只会让每次 API 调用都背上几十毫秒 | 是（仅会话） | 已实现 | F-1-10 |
| API-172 | DELETE | `/api/v1/api-keys` | 撤销凭证。**不删记录**、只写 `revoked_at`：留着才能回答"这个 Key 是什么时候被撤掉的"，而那正是出事之后要查的东西；删掉它，用户看到的是"我从没生成过 Key" | 是（仅会话） | 已实现 | F-1-10 |
| API-173 | POST | `/api/v1/action-tokens` | 签发**一次性动作令牌**。它解决的是「浏览器之外的下载」：`<a href>` 带不上自定义请求头，因此用 Cookie 认证的接口没法直接下给用户。三条约束缺一不可：**一次性**（它会出现在 URL、浏览器历史、日志、Referer 里）、**短期**（5 分钟）、**绑定用途** | 是（仅会话） | 已实现 | F-1-10 |
| API-150 | GET | `/api/v1/firewall/policy` | 节点级防火墙策略（F-4-11 双层的第一层）。**首次访问会建一条默认关闭的策略**——默认建一条启用的 deny 会让所有未配规则的节点立刻开始拦截流量，包括面板自己，那是一次由「打开页面」触发的停机 | 是（管理员） | 已实现 | F-4-11 |
| API-151 | PATCH | `/api/v1/firewall/policy` | 更新策略（启用、默认处置、区域限制、白名单）。**只改配置，不下发**——下发有真实的网络影响，而改配置不该有；合成一步会让「我想先看看改完是什么样」变得不可能 | 是（管理员） | 已实现 | F-4-11 |
| API-152 | GET | `/api/v1/firewall/rules` | 节点级规则列表。保护规则排在最前 | 是（管理员） | 已实现 | F-4-11 |
| API-153 | POST | `/api/v1/firewall/rules` | 新增规则。去重按 `coalesce(port_start,0)` 等归一化口径比对（见下方说明）。ICMP 与「全部」协议拒绝填端口；来源必须是合法 IP 或 CIDR（裸地址会自动补 `/32` 或 `/128`） | 是（管理员） | 已实现 | F-4-11 |
| API-154 | DELETE | `/api/v1/firewall/rules/:ruleID` | 删除规则。**保护规则拒绝删除**，且由**服务端**强制——一次「清理规则」的操作就能把 SSH 或面板端口关掉，而那种事故**无法通过面板恢复**（那时已经连不上了，只能上宿主机敲命令）。让界面决定能不能删，等于把一个不可恢复的操作交给一次点击 | 是（管理员） | 已实现 | F-4-11 |
| API-155 | GET | `/api/v1/vms/:id/firewall` | 一台虚拟机**实际受到**的约束（双层合并的结果）。虚拟机级覆盖是**替换**而不是叠加——叠加会让「这台机器到底受哪些约束」变成两个列表的并集，而排查时需要在两份配置之间来回对照 | 是（管理员） | 已实现 | F-4-11 |
| API-156 | PUT | `/api/v1/vms/:id/firewall` | 设置虚拟机级覆盖。**项未设置即为「没有」**，而不是「沿用节点级」——只在非 nil 时才替换是一个看起来保守、实际更危险的实现：用户清空区域限制，得到的却是节点级的继续生效，而这台机器看起来已经「有自己的策略」了 | 是（管理员） | 已实现 | F-4-11 |
| API-157 | DELETE | `/api/v1/vms/:id/firewall` | 移除覆盖，回落到节点基线 | 是（管理员） | 已实现 | F-4-11 |
| API-158 | GET | `/api/v1/firewall/precheck` | 预检一次待应用的改动**会不会切断管理通道**。检查四项：当前访问来源是否会被挡掉、默认拒绝且白名单为空、保护规则是否缺失、区域限制与白名单的关系。返回警告而不是拒绝——有时「就是要挡掉这些」是用户的真实意图 | 是（管理员） | 已实现 | F-4-11 |
| API-159 | POST | `/api/v1/firewall/apply` | 下发策略到宿主机。**有警告而未确认时不下发、也不报错**，而是把警告原样返回——报错会让界面把它显示成一次失败，而它实际是一个需要用户做决定的岔路口。**同步调用节点、不经队列**：管理员点下按钮后立刻需要一个答复 | 是（管理员） | 已实现 | F-4-11 |
| API-160 | POST | `/api/v1/firewall/rollback` | **紧急回滚**：一键关闭防火墙。**不做确认、不做预检、不需二次验证**。这个「不做」是有意的**不对称**——通往事故的路要设卡（应用要确认警告），从事故里出来的路不能设卡；被自己配错的防火墙关在门外的管理员，此刻唯一的诉求是「先让我进去」，任何一道额外确认都会成为压垮他的那一步 | 是（管理员） | 已实现 | F-4-11 |
| API-148 | PUT | `/api/v1/my-storage/uploads/:uploadID/chunks/:index` | 接收分片**字节**（F-5-04）。请求体是**裸二进制**而不是 JSON——把它 base64 包进 JSON 会让体积涨 33%，而一次 8MiB 的分片会因此多传 2.7MiB。**先落盘再登记**：反过来的话，登记成功而落盘失败会留下「bitmap 说收到了、磁盘上却没有」的缺口，而它要到完成拼接时才暴露。客户端给的摘要不匹配时**不登记**并让客户端重传——登记了再报错等于把坏数据留在暂存区。分片独立成文件（不是按偏移写进同一个大文件）：后者要预先撑到完整大小，而一个 40GB 的空洞会立刻被配额统计算进去 | 是 | 已实现（字节层已落地） | F-5-04 |
| API-140 | GET | `/api/v1/my-storage` | 用户在指定节点上的存储空间状态。存储空间**按节点**开通——磁盘就在那台宿主机上，一个用户在 A 节点的文件与 B 节点无关。未开通时返回 `enabled: false`，界面据此引导开通而不是显示一个空列表（「列表为空」与「你没开通」看起来一样） | 是 | 已实现 | F-5-03 |
| API-141 | POST | `/api/v1/my-storage` | 开通存储空间。**幂等**：已开通时直接返回，不重复写库也不重复记审计。**配额为 0（不限）时也允许开通**——开通与限额是两件事，强行要求先设额度才能开通会让管理员在做任何事之前必须先去另一个页面填个数字 | 是 | 已实现 | F-5-03 |
| API-142 | GET | `/api/v1/my-storage/files` | 文件列表，可按 `category`（iso / share / disk）过滤。**只返回内容已就绪的文件**——受理中的上传在 `storage_file` 里也有一行记录，列出来会让用户看到一个大小正常、点下载却没有内容的条目，而他会先怀疑是下载坏了 | 是 | 已实现 | F-5-05 |
| API-143 | DELETE | `/api/v1/my-storage/files/:id` | 删除文件。**不需要二次验证**——删的是自己的文件，且随时可以重新上传；这里的不可逆只对**这一次**成立，内容本身在用户手上 | 是 | 已实现 | F-5-05 |
| API-144 | POST | `/api/v1/my-storage/uploads` | 创建上传会话，**可命中秒传**。**配额在授予上传之前检查**：一个 40GB 的上传传到一半才被告知超配额，用户已经等了几十分钟。**秒传的查重范围限定在同一用户内**——全局查重会让用户 A 通过「上传 → 秒传成功」推断出用户 B 是否存在某个文件，内容读不到但存在性已经泄漏，而对某些文件来说存在性本身就是敏感信息。分片数上限 10 万（1 字节分片会生成天文数字的 bitmap） | 是 | 已实现 | F-5-04 |
| API-145 | GET | `/api/v1/my-storage/uploads/:uploadID` | 查询会话状态，供断点续传。`missing_chunks` 返回**缺失分片的序号列表**——客户端要据此决定传哪几片，而「还差 3 片」它无法据以行动，只能重传全部。过期会话返回 `status: expired` 且**不给分片清单**：要区分「过期了，重传即可」与「从没存在过」，两者的处理方式完全不同 | 是 | 已实现 | F-5-04 |
| API-147 | POST | `/api/v1/my-storage/uploads/:uploadID/complete` | 收尾上传并登记文件。**配额在这里再检查一次**：只在创建会话时检查的话，「开始时还没超」就成了一个永久有效的通行证——用户可以先开会话、慢慢传几小时，期间其他操作把空间占满，最后落盘时依然被放行。分片未到齐时拒绝并给出缺口数量与前几个序号 | 是 | 已实现 | F-5-04 |
| API-140 | GET | `/api/v1/storage-volumes` | 节点上的存储卷（F-5-02，LVM 多盘聚合）。每个卷都返回 `has_redundancy` —— **把「能不能坏一块盘」从一个需要用户自己推的参数组合变成一个字段**。`status_note` 用人话解释状态：`sync`（镜像初始同步，写会明显变慢几小时）与 `degraded`（镜像缺一份，**看起来正常但已无冗余**）这两种如果只显示英文单词，用户不会知道要不要做什么 | 是（管理员） | 已实现 | F-5-02 |
| API-141 | POST | `/api/v1/storage-volumes/preview` | 创建前换算。**只读**，不产生改动、不入队。它回答三个最容易想错的数：需要几块盘（**stripe × mirror**，用户会想「镜像要 2、条带要 2，给 2 块就行了吧」）、实际占多少物理空间（镜像让它变成可用容量的倍数）、建成后有没有冗余。这些由服务端算而不是界面推——两处各算一遍迟早对不上 | 是（管理员） | 已实现 | F-5-02 |
| API-142 | POST | `/api/v1/storage-volumes` | 创建存储卷。**条带配置会给出警告并要求显式确认**——条带提升吞吐但**没有冗余**，任何一块盘故障都会让整个卷不可用，而且因为数据是分散的，剩下那些盘上的内容也无法单独恢复。这与「多块盘放在一起」给人的直觉正好相反。有警告而未确认时**不创建也不报错**，而是把计划原样返回 | 是（管理员） | 已实现 | F-5-02 |
| API-143 | DELETE | `/api/v1/storage-volumes/:id` | 删除存储卷。**会销毁卷里的全部数据**，且不可恢复——这是它与存储池最不同的一点：池是设备的组织方式，删掉只是「不再用这几块盘」；而卷里装的是真实数据。删除是**硬删除**（表里没有 `deleted_at`）：卷与其中的数据都已销毁，保留记录会让那些设备一直显示为被占用，用户再也建不了新卷 | 是（管理员） | 已实现 | F-5-02 |
| API-130 | GET | `/api/v1/vms/:id/shares` | 虚拟机的目录共享列表（F-5-06，9p VirtFS）。同时返回 `rel_path`（用户填的相对路径）与 `host_path`（拼出的绝对路径）——只给相对路径的话，用户得自己去拼，拼错的结果是他以为共享的是 A 目录、实际共享的是 B | 是 | 已实现 | F-5-06 |
| API-131 | POST | `/api/v1/vms/:id/shares` | 新增目录共享。**只接受相对于调用者存储根的路径**——这是整块功能的安全前提：共享是一个跨越虚拟化边界的读取入口，而参数来自用户，允许绝对路径意味着一个租户可以把 `/etc` 挂进自己的虚拟机读出来，且不触发任何权限检查（qemu 以有权读它的用户在跑）。逐层校验：拒绝绝对路径 → Clean 后不得以 `..` 开头 → 拼接后再做带分隔符的前缀检查。`read_only` **默认 true**（可写共享的写入不受配额约束），`passthrough`/`none` 仅管理员可用 | 是 | 已实现 | F-5-06 |
| API-132 | DELETE | `/api/v1/vms/:id/shares/:tag` | 卸载目录共享。**不需要二次验证**——目录还在宿主机上，随时可以再挂回去，而来宾里的失效是立刻可见的 | 是 | 已实现 | F-5-06 |
| API-110 | GET | `/api/v1/security-groups` | 安全组列表。含每个组的规则数与**挂载网口数**——后者是删除前的判断依据。租户只看自己的组与管理员的预置组 | 是 | 已实现 | F-4-03 |
| API-111 | POST | `/api/v1/security-groups` | 新建安全组。名称**在节点内唯一**（安全组是节点级资源），跨节点可重名 | 是 | 已实现 | F-4-03 |
| API-112 | DELETE | `/api/v1/security-groups/:id` | 删除安全组。**仍被网口挂载时拒绝并给出数量**——删掉一个正在被使用的组会静默放开一批机器的流量，那是一次方向与预期相反的变更。默认组不可删（新建网口依赖它） | 是 | 已实现 | F-4-03 |
| API-113 | GET | `/api/v1/security-groups/:id/rules` | 组内规则。按方向 + 优先级 + id 排序：优先级相同的规则需要一个稳定次序，否则同一份数据每次刷新顺序都可能不同 | 是 | 已实现 | F-4-03 |
| API-114 | POST | `/api/v1/security-groups/:id/rules` | 新增**允许**规则。**组内没有拒绝规则**——叠加生效的并集模型里「拒绝」没有确定含义（A 组拒绝 22、B 组允许 22，合并后通不通取决于谁先算）。ICMP 与「全部」协议**拒绝填写端口**：填了会得到一条看起来有限制、实际没有的规则。CIDR 目标必须显式填写（要放行任意来源请写 `0.0.0.0/0`，「留空」与「忘了填」在界面上分不开） | 是 | 已实现 | F-4-03 |
| API-115 | PATCH | `/api/v1/security-groups/:id/rules/:ruleID` | 修改规则。**用修改而不是删旧建新**：重建会得到新 id，而「这条规则被改过」与「被删掉又加了一条」是两件不同的事——追查一个放行了不该放行的端口时，前者能直接回答「谁在什么时候把它从 443 改成了 22」 | 是 | 已实现 | F-4-03 |
| API-116 | DELETE | `/api/v1/security-groups/:id/rules/:ruleID` | 删除规则 | 是 | 已实现 | F-4-03 |
| API-117 | POST | `/api/v1/security-groups/:id/interfaces/:interfaceID` | 把安全组挂到网口（**附加组**）。与网口上的主组合并生效。已是主组时拒绝——生效规则取并集，重复挂载不改变结果，却会让界面出现两个相同的组标签 | 是 | 已实现 | F-4-03 |
| API-118 | DELETE | `/api/v1/security-groups/:id/interfaces/:interfaceID` | 解除挂载。**主组不能从这里解除**——主组挂在 `vm_interface.security_group_id` 上，解除它等于把网口变成「没有安全组」，而那与「不限制」不是一回事 | 是 | 已实现 | F-4-03 |
| API-119 | GET | `/api/v1/vms/:id/security-groups/effective` | **汇总生效规则**（F-4-04）。把该虚拟机所有网口上的主组与附加组**合并去重**，并标出每条规则来自哪些组——否则用户看到合并结果后不知道该去哪儿改，只能在 5 个组里逐个翻。返回 `version`（快照指纹），应用时必须带回 | 是 | 已实现 | F-4-04 |
| API-120 | POST | `/api/v1/vms/:id/security-groups/apply` | 把生效规则下发到节点。**必须携带预览时的 version**，不匹配返回 409 而**不会**自动用最新规则继续——自动继续等于替用户批准了他没看过的东西。用户看到预览、判断没问题、点确认，这中间别人完全可能改了其中一个组 | 是 | 已实现 | F-4-04 |
| API-100 | GET | `/api/v1/public-ips` | 节点上的公网地址池。含每个地址的当前绑定（指向哪台虚拟机、模式、**节点上的实际生效状态**）。地址与虚拟机名一次查完——逐个查会把一次列表请求变成上百次往返，而一个 /24 就是 254 条 | 是（管理员） | 已实现 | F-4-06 |
| API-101 | POST | `/api/v1/public-ips` | 录入地址。支持单个地址或 **CIDR 批量展开**，自动跳过网络地址、广播地址与网关——它们不能分配给虚拟机，不过滤的话用户会得到一个看起来正常、绑定时才失败的地址。一次上限 /20（4096 个）：/8 这种多半是掩码写错了一位。IPv6 拒绝 1:1 NAT（地址本就充足） | 是（管理员） | 已实现 | F-4-06 |
| API-102 | DELETE | `/api/v1/public-ips/:id` | 从池中移除。**已绑定时拒绝**——移除一个正在被使用的地址会让绑定记录指向一条不存在的地址，而节点上那条规则仍然生效，两边就此分叉且都不报错 | 是（管理员） | 已实现 | F-4-06 |
| API-103 | POST | `/api/v1/public-ips/:id/bind` | 绑定到虚拟机。**已绑定时拒绝并指向「迁移」**——静默改绑会让「这个地址原来指向谁」在一次点击里消失，而如果用户点错了一行，他连错在哪里都看不到。校验同节点（跨节点没有网络路径）与地址声明的支持模式 | 是（管理员） | 已实现 | F-4-06 |
| API-104 | POST | `/api/v1/public-ips/:id/migrate` | **浮动迁移**——故障转移的实现方式：把地址从主机器挪到备机，外部访问几乎无感。必须原子（解绑+绑定在一个事务里），中途失败会让地址悬空而那时访问已经断了，用户却以为迁移还在进行。`mode` 留空时沿用当前模式 | 是（管理员） | 已实现 | F-4-06 |
| API-105 | DELETE | `/api/v1/public-ips/:id/bind` | 解绑，地址退回可用池。**不需要二次验证**——连通性中断是立刻可见的，用户马上就知道发生了什么 | 是（管理员） | 已实现 | F-4-06 |
| API-106 | GET | `/api/v1/public-ips/:id/preview` | 规则预览。**只读探测**，不产生任何改动、不入队。必须来自节点：规则的最终形态取决于宿主机上已有的链、路由表与网卡配置，控制面按模板拼一段出来看起来对、实际可能相差很远，而用户正是拿它做「改还是不改」的判断 | 是（管理员） | 已实现 | F-4-06 |
| API-090 | GET | `/api/v1/quota` | 当前用户在各节点上的存储用量与配额。**不传 user_id——只能查自己的**：少一个「用别人的 ID 去查」的入口，就少一次归属校验被写错的机会。用量**现算不读缓存**：按偏小的旧值放行会让用户悄悄超额，按偏大的旧值拦截会让他莫名被拒 | 是 | 已实现 | F-9-02 |
| API-091 | GET | `/api/v1/quotas` | 全部配额记录（管理员）。用量会暴露「谁有多少资源」，因此仅管理员可见 | 是（管理员） | 已实现 | F-9-02 |
| API-092 | PUT | `/api/v1/quotas` | 设置配额。**同步生效、不需要二次验证**——纯控制面元数据，随时可改回。`quota_bytes=0` 或 `enabled=false` 表示**不限**（面板默认状态是「先能用」，限额由管理员显式设置） | 是（管理员） | 已实现 | F-9-02 |
| API-088 | GET | `/api/v1/vms/:id/guest-actions` | 当前可用的来宾自动化动作（F-2-10）。由**后端按当前状态算好下发**，界面不自行判断——可用性与「是否装了 Guest Agent」「是否运行中」都相关，前端各判一遍迟早不一致，那会出现「按钮可点、点下去被拒」 | 是 | 已实现 | F-2-10 |
| API-089 | POST | `/api/v1/vms/:id/guest-actions` | 执行来宾自动化：在线改密（需运行中 + Agent）、离线改密（需关机，**不用** Agent）、附加磁盘自动分区挂载（需运行中 + Agent，**格式化不可逆**）、系统盘扩容（需关机 + Agent）。**密码入队供执行器取用，执行完成后立即从任务参数清除，审计流水里不含它**（R-009）。不需要二次验证：四种动作都可逆（密码能改回来、新盘不影响既有数据、扩容只变大） | 是 | 已实现 | F-2-10 |
| API-084 | GET | `/api/v1/vms/:id/exports` | 导出记录列表。记录在**受理时**即创建（pending）——导出可能跑几十分钟，界面要能看到「有一个导出在进行」，否则用户会以为没点上而重复发起 | 是 | 已实现 | F-2-14 |
| API-085 | POST | `/api/v1/vms/:id/exports` | 发起导出（`qcow2` / `ova`，可选数据盘）。**要求关机**，理由比其它操作更强：产物会被搬到别处使用，必须干净自洽。同一虚拟机同时只允许一个导出（占用同一份配额） | 是 | 已实现 | F-2-14 |
| API-086 | DELETE | `/api/v1/vms/:id/exports/:exportID` | 删除导出产物。进行中时拒绝。产物是副本，**不需要二次验证**；软删除以保留配额核销记录 | 是 | 已实现 | F-2-14 |
| API-087 | GET | `/api/v1/vms/:id/exports/:exportID/download` | 下载导出产物。返回**产物字节**而非节点直链——直链要把节点凭据或匿名地址暴露出去，而产物里是整台机器的数据。文件名按 RFC 6266 双段给出（兼容非 ASCII） | 是 | 已实现 | F-2-14 |
| API-083 | GET | `/api/v1/nodes/:id/stats` | 宿主机实时指标（CPU / 负载 / 内存 / 存储 / 运行时长）。只读探测，**不入队、不写投影**。虚拟机数量由**控制面统计**而非问节点——面板上显示的数字必须与列表页一致。未装配采集时明确返回 503 而不是一排 0（0% 看起来是「机器很闲」，而实际是「没读到」） | 是（管理员） | 已实现 | F-6-03 |
| API-006 | POST | `/api/v1/auth/login` | 用户名密码登录；令牌经 **HttpOnly Cookie** 下发，响应只含用户信息 | 否（公开，理由见 ADR-0008 同类的自举问题） | 已实现 | F-1-01 |
| API-007 | POST | `/api/v1/auth/logout` | 登出当前会话（仅撤销当前会话，并清除 Cookie） | 是 | 已实现 | F-1-02 |
| API-008 | GET | `/api/v1/auth/session` | 当前会话与用户信息 | 是 | 已实现 | F-1-02 |
| API-009 | GET | `/api/v1/auth/sessions` | 会话与登录记录列表（含当前会话标记，响应不含 session_id） | 是 | 已实现 | F-1-02 |
| API-010 | DELETE | `/api/v1/auth/sessions/:id` | 撤销指定会话（越权与不存在均返回 404）；**高风险操作，需二次验证** | 是 | 已实现 | F-1-02 |
| API-011 | GET | `/api/v1/tasks` | 任务列表（按状态 / 类型 / 资源筛选，归属过滤） | 是 | 已实现 | F-7-02 |
| API-012 | GET | `/api/v1/tasks/:id` | 任务详情（含参数、结果、错误与当前阶段）。**参数已脱敏**：口令、密钥、令牌不落库（R-009）。阶段**时间线**尚未实现——`task_stage` 表已在库里，但队列只写单个 `current_stage` 字符串 | 是 | 已实现（阶段时间线待补） | F-7-02 |
| API-013 | POST | `/api/v1/tasks/:id/cancel` | 请求取消任务（下发给执行方，**已产生的副作用不回滚**） | 是 | 已实现 | F-7-02 |
| API-014 | DELETE | `/api/v1/tasks` | 清理终态任务（拒绝含非终态） | 是 | 规划中 | F-7-02 |
| API-015 | GET | `/api/v1/events/{channel}` | **SSE 实时通道**（`vm-list` / `vm-detail` / `tasks` / `host-metrics`） | 是 | 规划中 | F-7-03 |
| API-016 | GET | `/api/v1/nodes/:id/disks` | 块设备清单（容量、状态标签、是否系统盘、是否含数据）；由 agent **实时探测**，不缓存 | 是（管理员） | 已实现 | F-5-01 |
| API-017 | GET | `/api/v1/nodes/:id/storage-pools` | 该节点的存储池列表（含空间与新鲜度） | 是（管理员） | 已实现 | F-5-01 |
| API-018 | POST | `/api/v1/storage-pools` | 创建存储池（格式化 + 挂载），返回任务标识；**高风险操作**，需二次验证 + `confirm_device_name` 逐字符一致 | 是（管理员） | 已实现 | F-5-01 |
| API-019 | PATCH | `/api/v1/storage-pools/:id` | 设为默认池（**同步完成**，不入队——只改控制面元数据） | 是（管理员） | 已实现 | F-5-01 |
| API-020 | DELETE | `/api/v1/storage-pools/:id` | 删除存储池（删除前向节点探测占用，有磁盘则拒绝并列出），返回任务标识；**高风险操作** | 是（管理员） | 已实现 | F-5-01 |
| API-021 | GET | `/api/v1/nodes/:id/network` | 网络后端模式与能力清单；能力为**三态**（可用 / 不可用 / 未知），缺失项附原因、影响范围与修复方式 | 是（管理员） | 已实现 | F-4-01 |
| API-022 | GET | `/api/v1/nodes/:id/networks` | 该节点可用网络列表（M2 仅系统基础网络）；首次查询时**幂等**建立系统网络 | 是（管理员） | 已实现 | F-4-01 |
| API-023 | GET | `/api/v1/vms/create-form` | 创建向导的表单元数据（字段、联动规则、可选值、前置条件） | 是 | 规划中 | F-2-02 |
| API-024 | POST | `/api/v1/vms` | 创建虚拟机（单台；批量走 API-028），返回任务标识 | 是 | 已实现 | F-2-02 |
| API-025 | GET | `/api/v1/vms` | 虚拟机列表（筛选 / 排序 / 分页，含数据新鲜度与可用操作） | 是 | 已实现 | F-2-01 |
| API-026 | GET | `/api/v1/vms/:id` | 虚拟机详情（配置、投影状态、最近同步时间与可用操作） | 是 | 已实现 | F-2-03 |
| API-027 | POST | `/api/v1/vms/:id/power-actions` | 电源操作（`start` / `shutdown` / `reboot` / `poweroff` / `reset`）；受理时**基于实时探测校验状态**，返回任务标识 | 是 | 已实现 | F-2-04 |
| API-028 | POST | `/api/v1/vms/batch-actions` | 批量操作：电源（`start` / `shutdown` / `poweroff` / `reset`）与删除（`delete` + 必填 `disk_action`）。**逐台独立受理、可部分成功**，因此始终返回 200，逐台结果在 `items` 里；失败项带具体原因（含被锁定项）。同一 ID 会被去重，单次上限 50 台。删除动作走**一次**二次验证（不是每台一次） | 是（删除需验证） | 已实现 | F-2-01 |
| API-029 | DELETE | `/api/v1/vms/:id` | 删除虚拟机（`disk_action` 必填：`delete` / `keep`，服务端不设默认值；走 query 或 body 均可）；**高风险操作，需二次验证** | 是 | 已实现 | F-2-04 |
| API-030 | GET | `/api/v1/vms/:id/console` | 控制台配置与状态（开启、端口、**监听地址**、暴露状态、显示设备、会话数与上限、流支持）；**不含密码** | 是 | 已实现 | F-2-08 |
| API-031 | PATCH | `/api/v1/vms/:id/console` | 开启/关闭、设置密码、切换对外暴露；**仅在变更暴露状态时**要求二次验证；密码最长 8 位且只写不读 | 是 | 已实现 | F-2-08 |
| API-032 | GET | `/api/v1/vms/:id/console/screenshot` | 控制台截帧预览；**当前返回 503**——截帧需 agent 侧图形导出能力，该契约尚未定义 | 是 | 已实现（能力待 agent） | F-2-08 |
| API-033 | GET | `/api/v1/vms/:id/console/ws` | **WebSocket**：VNC 流量代理（经 agent 通道转发，不直连宿主机）；**升级前**完成鉴权与授权，会话数上限 3 | 是 | 已实现 | F-2-08 |
| API-030 | PUT | `/api/v1/auth/password` | 修改自己的密码（F-1-03）。**必须提供当前密码**——拿到未锁屏的浏览器或偷到会话令牌就能改掉密码并把主人锁在外面，而那时主人连「怎么进不去了」都查不出来（会话是合法的，日志里看不出异常）。改完**撤销其它会话但保留当前这一个**：改密码最常见的动机就是「我怀疑账号被盗」，旧会话还活着这个动作就白做了；而连当前会话也撤掉，用户会以为改密码失败。旧密码下签发的一次性许可也一并作废 | 是 | 已实现 | F-1-03 |
| API-031 | PUT | `/api/v1/auth/username` | 修改自己的登录名。需要当前密码，但**不撤销会话**——用户名只是标识，密码才是凭据，把它写得和改密码一样严重反而会让人不敢改。冲突时不说「这个名字被谁用了」（那是用户名枚举的入口，f-1-06 R-005）。审计里记下**改之前叫什么**：事后追查时旧名字是唯一能对上的线索 | 是 | 已实现 | F-1-03 |
| API-032 | POST | `/api/v1/auth/recovery-codes` | 重新生成恢复码（F-10-01）。**旧码全部作废**——发一批新的「万能钥匙」而旧的还能用，等于把可用入口翻了一倍，而用户以为只换了新的那张纸。**刻意不要求二次验证**：用户重新生成恢复码的常见原因恰恰是「手机丢了、恢复码快用完了」，那时他刚用一个恢复码登进来，要求 TOTP 就是要求他拿出已经丢了的东西 | 是 | 已实现 | F-10-01 |
| API-034 | GET | `/api/v1/security/high-risk-policy` | 高风险操作清单与判定口径（供前端渲染提示；**清单由后端下发，前端不得硬编码第二份**） | 是 | 已实现 | F-10-02 |
| API-035 | POST | `/api/v1/auth/risk-verification` | 提交验证码，换取一次性高风险许可（2 分钟有效、消费即失效、绑定会话）；`method` 由**后端下发的可用方式列表**决定，其中 `dev_bypass` 仅在开发模式出现 | 是 | 已实现 | F-10-01 |
| API-036 | GET | `/api/v1/settings` | 设置项清单：**元数据 + 当前生效值 + 来源**（环境变量 / 面板设置 / 默认值）；未交付标签的项不出现 | 是（管理员） | 已实现 | F-9-01 |
| API-037 | PATCH | `/api/v1/settings` | 批量更新设置；**部分成功**语义，逐项返回「已生效 / 失败 / 已回滚」；被环境变量锁定的项一律拒绝 | 是（管理员） | 已实现 | F-9-01 |
| API-038 | POST | `/api/v1/settings/rollback` | 将指定设置项回滚到最近一次变更前的值（只保留一次前值，回滚本身也可再回滚） | 是（管理员） | 已实现 | F-9-01 |
| API-039 | GET | `/api/v1/setup/status` | 系统是否已完成初始化（供前端决定跳初始化页或登录页） | 否（公开，理由见 [ADR-0008](../06-decisions/0008-first-admin-bootstrap.md)） | 已实现 | 首次初始化 |
| API-040 | POST | `/api/v1/setup/admin` | 用一次性令牌创建首个管理员；成功后自动建立会话 | 否（公开，理由同上） | 已实现 | 首次初始化 |
| API-041 | GET | `/api/v1/auth/security-setup` | 二次验证方式的绑定进度与可用方式；`dev_bypass` 回报开发期万能码是否启用（由后端下发，前端不得自行推断，否则会出现「界面说没开、实际开着」） | 是 | 已实现 | F-10-01 |
| API-042 | POST | `/api/v1/auth/totp/setup` | 生成 TOTP 密钥并返回 otpauth URI（**未启用**，需确认） | 是 | 已实现 | F-10-01 |
| API-043 | POST | `/api/v1/auth/totp/confirm` | 提交动态码启用绑定，并返回恢复码（**只返回一次**） | 是 | 已实现 | F-10-01 |
| API-044 | GET | `/api/v1/storage-pools/:id` | 单个存储池详情（配置、容量与新鲜度） | 是（管理员） | 已实现 | F-5-01 |
| API-045 | POST | `/api/v1/dev/agent-register` | 开发期模拟 agent 注册；**仅在 `AGENT_TRANSPORT=mock` 时注册该路由**，接入真实 agent 后不存在 | 否（开发期） | 已实现（开发期专用） | F-6-01 |
| API-046 | GET | `/api/v1/vms/:id/interfaces` | 虚拟机网卡列表（型号、MAC、接入网络、限速、下发状态）。`order` 是网卡在**来宾系统内的设备顺序**，界面应以它而非 `id` 为主标识。**只读**：增删改需下发到节点，尚未实现 | 是 | 已实现（只读） | F-2-03 |
| API-047 | GET | `/api/v1/vms/:id/static-ips` | 虚拟机已绑定的静态地址，区分 DHCP 静态租约与来宾内手工配置（两者的排查方向不同） | 是 | 已实现（只读） | F-2-03 |
| API-048 | GET | `/api/v1/vms/:id/schedules` | 定时任务列表（动作、计划、下次执行、上次结果与产生的任务标识）。`last_result` 取值含 `skipped`——服务停机期间错过的时间点**不补执行**，界面必须显示它 | 是 | 已实现 | F-7-05 |
| API-049 | POST | `/api/v1/vms/:id/schedules` | 新建定时任务；**删除类仅允许一次性**，且在**创建时**走二次验证——它是一条将来会自动执行的删除指令，留到执行时再验证就没人可验了 | 是（删除类需二次验证） | 已实现（界面暂未接入删除类） | F-7-05 |
| API-050 | PATCH | `/api/v1/vms/:id/schedules/:scheduleID` | 启用 / 停用定时任务。停用**保留记录与执行历史**（用户能看出它跑过），不同于删除 | 是 | 已实现 | F-7-05 |
| API-051 | DELETE | `/api/v1/vms/:id/schedules/:scheduleID` | 删除**定时任务定义**，不删除虚拟机。可逆性完全不同，因此不需要二次验证 | 是 | 已实现 | F-7-05 |
| API-052 | GET | `/api/v1/vms/:id/snapshots` | 快照列表与配额（`used` / `quota`）。每条带后端算好的 `can_delete` / `can_restore`——**界面不得自行拼这套判断**，否则规则存在两份、迟早不一致 | 是 | 已实现 | F-2-07 |
| API-053 | POST | `/api/v1/vms/:id/snapshots` | 创建快照。快照种类由后端按「是否含内存 + 当前运行态」**推导**，不是用户选的 | 是 | 已实现 | F-2-07 |
| API-054 | POST | `/api/v1/vms/:id/snapshots/:snapshotID/restore` | 恢复快照。会**丢弃快照之后的所有磁盘改动**，属于不可逆操作，因此走二次验证。注意与「删除快照」（API-055）的区别：后者只失去一个还原点，当前数据不受影响，**不需要**验证 | 是 | 已实现 | F-2-07 |
| API-055 | DELETE | `/api/v1/vms/:id/snapshots/:snapshotID` | 删除快照。有子快照或正处于「当前状态」时拒绝，并给出**具体**原因 | 是 | 已实现 | F-2-07 |
| API-056 | GET | `/api/v1/vms/:id/edit-form` | 编辑页的表单元数据与当前值。含**运行态可改矩阵**（`requires_node` / `requires_shutdown` / `read_only`）与子选项卡定义——两者都是单一事实来源，前端不得硬编码第二份 | 是 | 已实现（覆盖 6 个子选项卡） | F-2-05 |
| API-057 | PATCH | `/api/v1/vms/:id/metadata` | 修改备注、分组。**同步生效、不入队**：纯控制面数据，虚拟化层不知道它们的存在，因此运行中也能改 | 是 | 已实现 | F-2-05 |
| API-058 | POST | `/api/v1/vms/:id/config-changes` | 提交配置变更（`changes` 为字段集合，形状**由矩阵决定**）。只接受真正变化的字段；不在矩阵里的字段、只读字段、非法枚举一律拒绝；互斥项（IOPS 总量与读写分离）校验**变更后的最终状态** | 是 | 已实现 | F-2-05 |
| API-059 | POST | `/api/v1/vms/:id/interfaces` | 新增网卡。序号取「当前最大 + 1」而非已有数量；MAC 由控制面生成并**固定**（重建后不变，否则来宾里按 MAC 配的网络会失效） | 是 | 已实现 | F-2-03 |
| API-060 | PATCH | `/api/v1/vms/:id/interfaces/:nicID` | 修改网卡的型号、限速与接入网络。**序号与 MAC 不可改**：前者是来宾里的设备顺序，后者可能已被按它配置过 | 是 | 已实现 | F-2-03 |
| API-061 | DELETE | `/api/v1/vms/:id/interfaces/:nicID` | 删除网卡。**主网卡拒绝**：重装系统依赖它保持网络可达 | 是 | 已实现 | F-2-03 |
| API-062 | POST | `/api/v1/vms/:id/static-ips` | 绑定静态地址。地址在**节点内唯一**，冲突时返回 409 而不是让它静默失效 | 是 | 已实现 | F-2-03 |
| API-063 | DELETE | `/api/v1/vms/:id/static-ips/:ipID` | 解绑静态地址。只解除控制面的分配关系，不改动来宾内已有的配置 | 是 | 已实现 | F-2-03 |
| API-064 | GET | `/api/v1/vms/:id/port-forwards` | 端口转发列表。`applied` 为 false 表示规则**尚未下发、当前并不生效** | 是 | 已实现 | F-2-03 |
| API-065 | POST | `/api/v1/vms/:id/port-forwards` | 新增端口转发。端口在**节点内独占**（`(node_id, protocol, host_port)`）；`allowed_ips` 留空表示**不限制来源**，界面须显式提示 | 是 | 已实现 | F-2-03 |
| API-066 | DELETE | `/api/v1/vms/:id/port-forwards/:pfID` | 删除端口转发。删除的是宿主机上的规则，虚拟机的服务不受影响 | 是 | 已实现 | F-2-03 |
| API-067 | PATCH | `/api/v1/vms/:id/lock` | 加锁 / 解锁业务软锁（F-2-12）。**同步生效、不入队**——锁只存在于控制面，虚拟化层不知道它的存在。**加锁不需验证，解锁需要**：锁的全部意义就是让删除必须先经过一道明确的动作，解锁若只是一次普通点击，它就只是减速带 | 是（仅解锁） | 已实现 | F-2-12 |
| API-068 | GET | `/api/v1/vms/:id/stats` | 实时运行指标（Hero 资源卡）。只读探测，**不入队、不写投影**——指标是瞬时的，存下来只会在下一次读取时给出过期的答案。响应含 `at`（采集时刻），界面据此标注新鲜度：轮询失败时不能把上一组数字当成当前的 | 是 | 已实现 | F-2-01 |
| API-069 | GET | `/api/v1/vms/:id/console/frame` | 控制台预览截帧（Hero 预览卡）。返回 **image/png 字节**而非 JSON——画面是二进制，塞进 JSON 只是多绕一圈。`display=none` 的虚拟机返回 422（判据与 `has_console` 一致：两处各判各的会出现「卡片上有预览、点进去没有控制台」） | 是 | 已实现 | F-2-01 |

> **公开接口共 4 个**（`/health`、`/api/v1/setup/*`、`/api/v1/auth/login`）。前两个的公开理由是「系统尚无可用凭据时的自举需要」：初始化接口靠**只能从服务端日志获取**的一次性令牌保护（[ADR-0008](../06-decisions/0008-first-admin-bootstrap.md)），登录接口是获取凭据的入口本身。新增公开接口必须在此说明理由。

### 5.1 开发专用接口

> 以下接口**不属于对外契约**，仅在开发期存在，接入真实 agent 后不再注册路由（[ADR-0007](../06-decisions/0007-mock-agent-first.md)）。

| 编号 | 方法 | 路径 | 说明 | 认证 | 状态 | 关联功能 |
|---|---|---|---|---|---|---|
| DEV-001 | POST | `/api/v1/dev/agent-register` | 模拟 agent 注册：调用与真实 agent **相同的注册逻辑**，使节点可在无 agent 的情况下接入 | 否（凭注册令牌，与真实 agent 一致） | 仅 `AGENT_TRANSPORT=mock` 时注册 | F-6-01 |

**状态口径**：

- `已实现` = 代码中已注册且可用；
- `规划中` = 已在功能规格中确定契约、尚未实现。

> 本表**允许先于实现登记**（契约先行），但状态必须如实标注——禁止把未实现的接口标为已实现，也禁止实现后不更新状态。
>
> **脚手架阶段的示例接口（`/api/v1/users` 系列）已随示例模型一并移除**——它与正式表结构不兼容，留着会让"按模型自动建表"给正式表加错列。
> 新增接口的顺序：先在功能规格（[`../07-specs/`](../07-specs/README.md)）确定契约 → 在本表登记 → 实现于 `internal/handler/` → 注册于 `internal/router/router.go`。

---

## 6. 接口详情模板

### <!-- 接口名 -->

- **方法与路径**：`GET /example`
- **说明**：
- **认证要求**：
- **请求参数**：

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| | | | | |

- **请求示例**：

```json
{}
```

- **响应示例**：

```json
{}
```

- **错误码**：
-

- **变更记录**：

| 日期 | 变更 | 兼容性 |
|---|---|---|
| | | |
