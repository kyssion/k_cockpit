# 安全策略

## 支持的版本

| 版本 | 是否维护 |
|---|---|
| main | 是 |
| <!-- TODO --> | |

## 报告安全漏洞

**请勿通过公开 Issue 报告安全问题。**

请通过以下方式私下联系维护者：

- 邮箱：<!-- TODO: security@example.com -->
- 或其他私密渠道：<!-- TODO -->

请在报告中包含：
1. 漏洞类型与影响范围
2. 复现步骤（含最小复现样例）
3. 可能的利用方式与危害评估
4. 已知的缓解措施（若有）

**响应时效承诺**：
- 24 小时内确认收到
- 7 个工作日内给出初步评估
- 修复后经你同意再公开披露

## 安全开发要求

所有贡献者（含使用 AI 工具生成代码的场景）必须遵守：

### 密钥与凭据
- 严禁将密钥、令牌、证书、数据库口令提交到仓库。
- 本地配置一律放在 `.env`（已被 `.gitignore` 忽略），仓库只保留 `.env.example`。
- 一旦怀疑密钥泄露，**立即轮换密钥**，仅删除提交是不够的（Git 历史仍可追溯）。

### 输入与输出
- 所有外部输入（请求参数、上传文件、第三方回调）必须校验与转义。
- 防范 SQL 注入：使用参数化查询，禁止字符串拼接 SQL。
- 防范 XSS：输出按上下文转义，谨慎使用 `innerHTML` / 动态执行。
- 防范 SSRF：对用户可控的 URL 做白名单校验。
- 防范越权：每次数据访问都要校验「当前用户是否有权限访问该资源」，不能只靠前端隐藏。

### 依赖与供应链
- 新增依赖前确认其维护状态、协议与已知 CVE。
- 定期执行依赖漏洞扫描（见 CI 配置）。

### 日志与数据
- 日志中禁止输出密码、令牌、身份证号、银行卡号等敏感信息。
- 测试数据必须脱敏，禁止使用生产数据副本。

### AI 生成代码的额外要求
- AI 生成的鉴权、加密、序列化、文件上传相关代码，必须**人工逐行审查**。
- 不得让 AI 自行「发明」加密算法或安全协议，必须使用成熟库的标准实现。
