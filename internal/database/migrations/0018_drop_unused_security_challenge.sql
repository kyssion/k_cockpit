-- 删除 security_challenge 表：它从未被读写过。
--
-- 这张表在设计阶段用于「二次验证挑战」，但实际实现（internal/risk）
-- 把挑战放在了别的地方，这张表从来没有被任何一行代码引用过。
--
-- 为什么不只是留着：一张**永远不会被读写**的表留在 schema 里是有害的，
-- 它会让后来的人以为那条路径存在——于是排查验证相关的问题时会先去看它的
-- 内容（空的），再怀疑是自己没找对地方；也可能有人照着它写代码，从而
-- 凭空造出第二条互不相关的验证路径。
--
-- 判断依据不是"看起来没人用"，而是全仓检索：`security_challenge` 在
-- Go 代码里出现 0 次（模型、查询、关联都没有）。
DROP TABLE IF EXISTS security_challenge;
