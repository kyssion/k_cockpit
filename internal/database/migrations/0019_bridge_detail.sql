-- 失败原因（F-4-13 的「可诊断」）。
--
-- 规格要求网络配置失败「必须可见、可诊断」。而桥的 status='error' 只说
-- 「出错了」——用户唯一的动作是重试，而重试一件已经失败的事通常不会得到
-- 不同结果。本列存的是**具体原因**，让用户能判断该修什么。
--
-- 这一列本该和上一条迁移一起加，但上一条已经应用过了，而**已应用的迁移
-- 不能再改**：库里的结构与代码假设的结构会就此分叉，而且分叉之后谁也说不
-- 清是哪一边对。迁移执行器会直接拒绝执行被改过的历史文件——这次是这个
-- 保护先拦住了我，然后才有了这个文件。
ALTER TABLE network_bridge ADD COLUMN IF NOT EXISTS detail text;
