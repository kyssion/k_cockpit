# 贡献指南

感谢参与 `k_cockpit`。本文档说明代码与文档的贡献流程。

---

## 1. 开始之前

- 阅读 [`AGENTS.md`](AGENTS.md)（项目规则、技术栈、红线）。
- 阅读 [`docs/04-engineering/DEVELOPMENT.md`](docs/04-engineering/DEVELOPMENT.md) 完成本地环境搭建。
- **使用 AI 工具开发的同学注意**：AI 生成的内容由**提交者本人**负责，需自行审查正确性与安全性。

## 2. 分支规范

```
main                  # 主分支，始终保持可发布状态
├── feat/<简短描述>    # 新功能
├── fix/<简短描述>     # 缺陷修复
├── docs/<简短描述>    # 文档
├── refactor/<描述>    # 重构
├── chore/<描述>       # 构建/依赖/工具
└── hotfix/<描述>      # 紧急修复
```

命名使用小写英文与连字符，例如 `feat/user-login`。

## 3. 提交信息规范

采用 [Conventional Commits](https://www.conventionalcommits.org/)：

```
<type>(<scope>): <简短描述>

[可选正文：说明动机与方案]

[可选脚注：BREAKING CHANGE / 关联 Issue]
```

**type 取值**：`feat` `fix` `docs` `style` `refactor` `perf` `test` `build` `ci` `chore` `revert`

**示例**：

```
feat(auth): 支持手机号验证码登录

fix(api): 修复分页参数越界导致的 500

docs(architecture): 补充缓存层设计说明
```

要求：
- 描述用中文或英文均可，但**同一仓库保持一致**。
- 一行不超过 72 字符。
- 一个提交只做一件事，禁止把格式化与功能改动混在一起。

## 4. 代码提交前检查（必做）

```bash
# TODO: 技术栈确定后补充实际命令
# 格式检查
# 静态检查
# 单元测试
```

提交前必须保证：
- [ ] 本地 lint 通过，无新增告警
- [ ] 新增/修改逻辑有对应测试
- [ ] 未提交任何密钥、`.env`、真实凭据
- [ ] 相关文档已同步更新（见 `AGENTS.md` 第 8 节）

## 5. Pull Request 流程

1. 从最新 `main` 切出分支。
2. 完成开发并自检。
3. 推送分支，按模板创建 PR。
4. PR 需满足：
   - 描述清楚「改了什么 / 为什么 / 如何验证」
   - 关联对应 Issue
   - CI 全部通过
   - 至少 **1 位** Reviewer 批准
5. 使用 **Squash Merge** 合并，保持 `main` 历史线性。

## 6. 代码审查关注点

- 正确性与边界条件（空值、并发、异常路径）
- 是否存在安全隐患（注入、越权、密钥泄露、SSRF）
- 是否引入了不必要的依赖或复杂度过高的实现
- 可测性与命名可读性
- 文档与注释是否匹配实际行为

## 7. 不接受的提交

- 大段无意义的格式化 diff
- 注释掉的死代码
- 未经讨论的架构级重写
- 缺少测试的核心逻辑
- 包含了构建产物或大体积二进制文件

## 8. 行为准则

保持专业与尊重。技术分歧以**数据与文档**为准，不以个人偏好争论。
